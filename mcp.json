{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "context-bridge": {
      "type": "stdio",
      "command": "./dist/mcp/server.js",
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}

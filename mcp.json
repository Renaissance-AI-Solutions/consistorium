{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "consistorium": {
      "type": "stdio",
      "command": "node",
      "args": ["./dist/mcp/server.js"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
